# Cloudflare Pages redirects
# Proxy API calls to Railway backend

# API endpoints
/api/*  https://api.maptoprint.com/api/:splat  200

# Proxy preview images from backend
/previews/*  https://api.maptoprint.com/previews/:splat  200

# Proxy poster downloads from backend
/posters/*  https://api.maptoprint.com/posters/:splat  200

# SPA fallback - all other routes serve index.html
# Note: Static files (examples/, *.js, *.css) are served directly by Cloudflare
/*  /index.html  200
