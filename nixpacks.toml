# Railway/Nixpacks configuration
# Let nixpacks auto-detect Python, add geo library dependencies via apt

[phases.setup]
aptPkgs = ["libgdal-dev", "libgeos-dev", "libproj-dev", "gdal-bin"]

[phases.build]
cmds = ["mkdir -p web/previews posters cache"]

[start]
cmd = "python start.py"
